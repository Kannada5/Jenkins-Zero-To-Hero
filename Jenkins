pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'first.java'
            }
        }
        stage('Run') {
            steps {
                sh 'java -cp src first'
            }
        }
    }
}
